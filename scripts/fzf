#!/usr/bin/env elispp
;; don't yet know why this breaks it
;; elissp-opts: -v

(condition-case ()
    (if *stdin-file*
        (ns/pick "fzf" (s-split "\n" (f-read *stdin-file*)))
      (ns/pick "fzf" (s-split-lines (sh "rg --files"))))
  (quit nil))
